import authHandler from "@/auth"; // Adjust the path based on your project structure

// Export GET and POST handlers for the API route
export const GET = authHandler;
export const POST = authHandler;
